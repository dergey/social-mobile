<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sergey.zhuravlev.mobile.social">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Social"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.registration.RegistrationActivity" android:theme="@style/Theme.Social.NoActionBar" />
        <activity android:name=".ui.registration.EmailConfirmActivity" android:theme="@style/Theme.Social.NoActionBar" />
        <activity android:name=".ui.profile.ProfileSettingActivity" android:theme="@style/Theme.Social.NoActionBar" />
        <activity android:name=".ui.profile.ProfileActivity" android:theme="@style/Theme.Social.NoActionBar" />
        <activity android:name=".ui.message.MessageActivity" />
        <activity android:name=".ui.friend.FriendActivity" android:theme="@style/Theme.Social.NoActionBar" />
        <activity android:name=".ui.login.LoginActivity" android:theme="@style/Theme.Social.NoActionBar" />
        <activity
            android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>